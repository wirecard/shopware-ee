@forlang
[{{ locale }}]
Creditor = "{{ strings.creditor }}"
CreditorID = "{{ strings.creditor_id_input }}"
Debtor = "{{ strings.debtor }}"
AccountOwner = "{{ strings.debtor_acc_owner }}"
IBAN = "{{ strings.iban }}"
BIC = "{{ strings.bic }}"
CheckboxText = "{{ strings.sepa_text_6 }}"
Cancel = "{{ strings.cancel }}"
Confirm = "{{ strings.text_confirm }}"
SepaMandateTitle = "{{ strings.sepa_mandate }}"
SepaMandateText = "{{ strings.sepa_mandate_text }}"
SepaPaymentFormHeader = "{{ strings.sepadd_data }}"
FirstName = "{{ strings.first_name_input }}"
LastName = "{{ strings.last_name_input }}"
DueDate = "{{ strings.mandate_due_date }}"
MandateId = "{{ strings.creditor_mandate_id }}"
SignatureDate = "{{ strings.mandate_signature_date }}"

@endforlang
